# Чек-лист релиза 1.6.0

Проверено 2026-08-02 до создания релизного тега. При подготовке следующего
релиза версия и результаты этого файла должны быть обновлены.

## Код и совместимость

- [x] Новый режим хранится отдельно от профиля и не включается автоматически по возрасту.
- [x] Для существующих установок по умолчанию сохраняется стандартный интерфейс.
- [x] Упрощённый режим применяется сразу и не требует перезапуска приложения.
- [x] Критические действия доступны через подписанные кнопки без обязательного свайпа или удержания.
- [x] Ручной ввод даты и времени синхронизирован с календарём и циферблатом без автоматического фокуса.
- [x] Модель режима не зависит от Android или UI; настройки остаются локальными.

## Метаданные

- [x] `versionCode` увеличен, `versionName` установлен в `app/build.gradle.kts`.
- [x] Текущая версия, возможности и имена APK обновлены в `README.md`.
- [x] Раздел версии с датой и полным списком изменений оформлен в `CHANGELOG.md`.
- [x] Версии в Gradle, README и changelog совпадают; соответствие тегу проверит CI.

## Проверки

- [x] Выполнен `clean testDebugUnitTest compileDebugAndroidTestKotlin lintDebug assembleDebug`.
- [x] `git diff --check` не находит ошибок.

Android Lint завершён без ошибок; блокирующих предупреждений нет.
