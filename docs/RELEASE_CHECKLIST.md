# Чек-лист релиза 1.5.0

Проверено 2026-08-02 до создания релизного тега. При подготовке следующего
релиза версия и результаты этого файла должны быть обновлены.

## Код и архитектура

- [x] Фактические пакеты и направления зависимостей сверены с `docs/ARCHITECTURE.md`.
- [x] `model` и `domain` не зависят от Android или UI.
- [x] Receivers, notifications и application-слой не импортируют UI.
- [x] Координация статусов вынесена из UI в application-слой и зависит от порта уведомлений, а не Android-реализации.
- [x] Символы без вызывающего кода проверены через CodeGraph и поиском по проекту.
- [x] Подтверждённо неиспользуемые Kotlin API удалены; Android Lint проверен.

## Метаданные

- [x] `versionCode` увеличен, `versionName` установлен в `app/build.gradle.kts`.
- [x] Текущая версия, возможности и имена APK обновлены в `README.md`.
- [x] Раздел версии с датой и полным списком изменений оформлен в `CHANGELOG.md`.
- [x] Версии в Gradle, README и changelog совпадают; соответствие тегу проверит CI.

## Проверки

- [x] Выполнен `clean testDebugUnitTest compileDebugAndroidTestKotlin lintDebug assembleDebug`.
- [x] `git diff --check` не находит ошибок.

Lint завершён без ошибок. Оставшиеся предупреждения относятся к доступным обновлениям
двух библиотек и обязательному для adaptive icon каталогу ресурсов; блокирующих проблем нет.
